
//  main() {
  // var studentNames = ["Muhammad","Usman","Haider","Ali","Umer"];
    // var value = studentNames[studentNames.length-2];
    //  studentNames.replaceRange(0, 2, ["Ahmed","Essa"]);
    // studentNames.replaceRange(studentNames.length-1, studentNames.length, ["Basit"]);
    //   print(studentNames);
  
// }

main(){
    var numbers = [4,5,1,3,2,6,8,7,9];
    // numbers.sort();
    /////////////////
    numbers.forEach((e) {print(numbers);});
    ///
    // numbers.removeWhere((e) => e >6 );
    // print(numbers);
}